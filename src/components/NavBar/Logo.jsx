/** @format */

import logo from "../../assets/logo-no-background.webp";

const Logo = () => {
	return (
    <img
      src={logo}
      alt="personal logo"
      className="logo"
    />
  );
};

export default Logo;
